# Discovery n8n

Use CodeRED > Register Discovery. Registre capabilities por `service`, no por nombre de workflow. CodeRED guarda `integration_capabilities`, `integration_services` e `integration_plugins`. Si cambia una URL de webhook, vuelva a publicar discovery.
