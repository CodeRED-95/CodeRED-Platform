import crypto from 'node:crypto';import type { IncomingMessage, ServerResponse } from 'node:http';import type { Config } from '../config/Config.js';import type { AgentStorage } from '../storage/AgentStorage.js';import { sign } from '../protocol/RequestSigner.js';import type { PairingService } from '../protocol/PairingService.js';import type { DiscoveryService } from '../protocol/DiscoveryService.js';import type { HeartbeatService } from '../protocol/HeartbeatService.js';import type { ReconnectionService } from '../protocol/ReconnectionService.js';
const nonces=new Map<string,number>(); let pairHits:number[]=[];
async function body(req:IncomingMessage){const chunks:Buffer[]=[];for await(const c of req)chunks.push(Buffer.from(c));return chunks.length?JSON.parse(Buffer.concat(chunks).toString('utf8')):{}}
function json(res:ServerResponse,status:number,payload:unknown){res.writeHead(status,{'content-type':'application/json'});res.end(JSON.stringify(payload))}
export function createRouter(config:Config,storage:AgentStorage,pairing:PairingService,discovery:DiscoveryService,heartbeat:HeartbeatService,reconnect:ReconnectionService){return async(req:IncomingMessage,res:ServerResponse)=>{try{const url=req.url?.split('?')[0]||'/'; if(req.method==='GET'&&url==='/v1/health')return json(res,200,{status:'ok',version:'1.0.0'}); if(req.method==='GET'&&url==='/v1/status'){const i=await storage.readIntegration();return json(res,200,{status:heartbeat.status,paired:!!i,integration_uuid:i?.integration_uuid||null,platform_url:config.platformUrl,agent_version:'1.0.0',protocol_version:i?.protocol_version||'1.0',last_heartbeat_at:heartbeat.lastHeartbeatAt,last_discovery_at:discovery.lastDiscoveryAt})} if(req.method==='POST'&&url==='/v1/pair'){const now=Date.now();pairHits=pairHits.filter(t=>now-t<60000);if(pairHits.length>=5)return json(res,429,{success:false,message:'Rate limit exceeded'});pairHits.push(now);const b=await body(req);return json(res,200,await pairing.pair(String(b.pair_code||'')))} if(req.method==='POST'&&url==='/v1/discovery/sync')return json(res,200,{success:true,registered:await discovery.sync(true)}); if(req.method==='POST'&&url==='/v1/heartbeat/send')return json(res,200,{success:await heartbeat.send(),status:heartbeat.status}); if(req.method==='POST'&&url==='/v1/reconnect')return json(res,200,{success:true,status:await reconnect.start()}); if(req.method==='POST'&&url==='/v1/integration/disconnect'){await storage.clearIntegration();heartbeat.status='unpaired';return json(res,200,{success:true})} if(req.method==='POST'&&url==='/v1/challenge'){const i=await storage.readIntegration();if(!i)return json(res,409,{success:false,message:'Unpaired'});const b=await body(req);if(!b.challenge_id||!b.challenge)return json(res,422,{success:false,message:'Invalid challenge'});if(b.expires_at&&Date.parse(b.expires_at)<Date.now())return json(res,422,{success:false,message:'Challenge expired'});if(nonces.has(b.challenge_id))return json(res,409,{success:false,message:'Challenge already used'});nonces.set(b.challenge_id,Date.now()+300000);return json(res,200,{challenge_id:b.challenge_id,challenge:b.challenge,signature:sign(i.shared_secret,String(b.challenge)),responded_at:new Date().toISOString()})} return json(res,404,{success:false,message:'Not found'})}catch(e:any){return json(res,500,{success:false,message:e.message||'Agent error'})}}}
