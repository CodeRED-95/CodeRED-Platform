const SECRET_KEYS = ['secret','shared_secret','sharedSecret','authorization','x-codered-signature','pair_code','pairCode','token'];
export class Logger { constructor(private level='info'){} info(event:string,data:Record<string,unknown>={}){this.write('info',event,data)} warn(event:string,data:Record<string,unknown>={}){this.write('warn',event,data)} error(event:string,data:Record<string,unknown>={}){this.write('error',event,data)} private write(level:string,event:string,data:Record<string,unknown>){console.log(JSON.stringify({ts:new Date().toISOString(),level,event,...(sanitize(data) as Record<string,unknown>)}))}}
export function sanitize(value: unknown): unknown { if(Array.isArray(value)) return value.map(sanitize); if(value&&typeof value==='object'){const out:Record<string,unknown>={}; for(const [k,v] of Object.entries(value)){out[k]=SECRET_KEYS.includes(k.toLowerCase())?'[redacted]':sanitize(v)} return out} return value }
