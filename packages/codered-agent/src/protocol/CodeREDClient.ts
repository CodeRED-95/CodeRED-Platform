import type { Config } from '../config/Config.js';import type { AgentStorage } from '../storage/AgentStorage.js';import { signedHeaders, stableJson } from './RequestSigner.js';
export class CodeREDClient{constructor(private config:Config,private storage:AgentStorage){} async pair(pairCode:string){const body=stableJson({pair_code:pairCode,instance_name:this.config.name,instance_url:this.config.publicUrl,environment:this.config.environment,n8n_version:null,connector_version:'codered-agent/1.0.0',protocol_version:'1.0'});return this.raw('POST','/api/v1/integrations/n8n/pair',body,false)} async signed(method:string,path:string,payload:unknown={}){const integration=await this.storage.readIntegration();if(!integration) throw new Error('Agent is unpaired');const body=method==='GET'?'':stableJson(payload);return this.raw(method,path,body,true)} private async raw(method:string,path:string,body:string,signed:boolean){const integration=signed?await this.storage.readIntegration():null;const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),this.config.requestTimeoutMs);try{const res=await fetch(this.config.platformUrl+path,{method,body:body||undefined,headers:signed&&integration?signedHeaders(integration,method,path,body):{'Content-Type':'application/json'},signal:controller.signal});const text=await res.text();const json=text?JSON.parse(text):{};if(!res.ok){const e=new Error('CodeRED request failed '+res.status) as Error & {status?:number;retryAfter?:string};e.status=res.status;e.retryAfter=res.headers.get('retry-after')||undefined;throw e}return json}finally{clearTimeout(timer)}}}
