import type { Config } from '../config/Config.js';import type { AgentStorage } from '../storage/AgentStorage.js';import { CodeREDClient } from './CodeREDClient.js';
export type AgentStatus='connected'|'degraded'|'disconnected'|'revoked'|'unpaired'|'requires_repairing';
export class HeartbeatService{lastHeartbeatAt:string|null=null;failures=0;latencyMs:number|null=null;status:AgentStatus='unpaired';constructor(private config:Config,private storage:AgentStorage,private client:CodeREDClient){} async send(){const integration=await this.storage.readIntegration();if(!integration){this.status='unpaired';return false}const start=Date.now();try{await this.client.signed('POST','/api/v1/integrations/n8n/heartbeat',{instance_uuid:integration.integration_uuid,agent_version:'1.0.0',connector_version:'codered-agent/1.0.0',protocol_version:'1.0',environment:this.config.environment,sent_at:new Date().toISOString(),services:['n8n']});this.latencyMs=Date.now()-start;this.lastHeartbeatAt=new Date().toISOString();this.failures=0;this.status='connected';return true}catch(e:any){this.failures++; if(e.status===401||e.status===403)this.status='requires_repairing'; else if(e.status===410)this.status='revoked'; else this.status=this.failures>3?'disconnected':'degraded'; return false}}}