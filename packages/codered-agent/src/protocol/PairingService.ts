import type { Config } from '../config/Config.js';import type { AgentStorage } from '../storage/AgentStorage.js';import { CodeREDClient } from './CodeREDClient.js';import { DiscoveryService } from './DiscoveryService.js';import { HeartbeatService } from './HeartbeatService.js';
export class PairingService{constructor(private config:Config,private storage:AgentStorage,private client:CodeREDClient,private discovery:DiscoveryService,private heartbeat:HeartbeatService){} async pair(pairCode:string){if(!pairCode)throw new Error('pair_code is required');const response=await this.client.pair(pairCode);const data=response.data;if(!data?.integration_uuid||!data?.shared_secret)throw new Error('Invalid pairing response');const pairedAt=data.paired_at||new Date().toISOString();await this.storage.saveIntegration({integration_uuid:data.integration_uuid,shared_secret:data.shared_secret,protocol_version:data.protocol_version||'1.0',paired_at:pairedAt,platform_url:this.config.platformUrl,agent_name:this.config.name,environment:this.config.environment,secret_version:1});const discovery_registered=await this.discovery.sync(true);const heartbeat_sent=await this.heartbeat.send();return{success:true,integration_uuid:data.integration_uuid,paired_at:pairedAt,discovery_registered,heartbeat_sent}}}